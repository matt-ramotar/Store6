package org.mobilenativefoundation.store6.devtools

import org.mobilenativefoundation.store6.core.StoreNamespace

// Temporary T1 compile probe; the planned devtools API is introduced by later tasks.
internal val devtoolsCoreDependencyProbe = StoreNamespace("devtools")
